{
  "name": "rob-bot-control-plane",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check:web": "find api docs-site -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test:web": "node --test web-tests/*.test.js",
    "build": "npm run check:web && npm run test:web"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.0"
  }
}
